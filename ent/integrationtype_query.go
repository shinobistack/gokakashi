// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/shinobistack/gokakashi/ent/integrations"
	"github.com/shinobistack/gokakashi/ent/integrationtype"
	"github.com/shinobistack/gokakashi/ent/predicate"
)

// IntegrationTypeQuery is the builder for querying IntegrationType entities.
type IntegrationTypeQuery struct {
	config
	ctx              *QueryContext
	order            []integrationtype.OrderOption
	inters           []Interceptor
	predicates       []predicate.IntegrationType
	withIntegrations *IntegrationsQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the IntegrationTypeQuery builder.
func (itq *IntegrationTypeQuery) Where(ps ...predicate.IntegrationType) *IntegrationTypeQuery {
	itq.predicates = append(itq.predicates, ps...)
	return itq
}

// Limit the number of records to be returned by this query.
func (itq *IntegrationTypeQuery) Limit(limit int) *IntegrationTypeQuery {
	itq.ctx.Limit = &limit
	return itq
}

// Offset to start from.
func (itq *IntegrationTypeQuery) Offset(offset int) *IntegrationTypeQuery {
	itq.ctx.Offset = &offset
	return itq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (itq *IntegrationTypeQuery) Unique(unique bool) *IntegrationTypeQuery {
	itq.ctx.Unique = &unique
	return itq
}

// Order specifies how the records should be ordered.
func (itq *IntegrationTypeQuery) Order(o ...integrationtype.OrderOption) *IntegrationTypeQuery {
	itq.order = append(itq.order, o...)
	return itq
}

// QueryIntegrations chains the current query on the "integrations" edge.
func (itq *IntegrationTypeQuery) QueryIntegrations() *IntegrationsQuery {
	query := (&IntegrationsClient{config: itq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := itq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := itq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(integrationtype.Table, integrationtype.FieldID, selector),
			sqlgraph.To(integrations.Table, integrations.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, integrationtype.IntegrationsTable, integrationtype.IntegrationsColumn),
		)
		fromU = sqlgraph.SetNeighbors(itq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first IntegrationType entity from the query.
// Returns a *NotFoundError when no IntegrationType was found.
func (itq *IntegrationTypeQuery) First(ctx context.Context) (*IntegrationType, error) {
	nodes, err := itq.Limit(1).All(setContextOp(ctx, itq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{integrationtype.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (itq *IntegrationTypeQuery) FirstX(ctx context.Context) *IntegrationType {
	node, err := itq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first IntegrationType ID from the query.
// Returns a *NotFoundError when no IntegrationType ID was found.
func (itq *IntegrationTypeQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = itq.Limit(1).IDs(setContextOp(ctx, itq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{integrationtype.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (itq *IntegrationTypeQuery) FirstIDX(ctx context.Context) string {
	id, err := itq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single IntegrationType entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one IntegrationType entity is found.
// Returns a *NotFoundError when no IntegrationType entities are found.
func (itq *IntegrationTypeQuery) Only(ctx context.Context) (*IntegrationType, error) {
	nodes, err := itq.Limit(2).All(setContextOp(ctx, itq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{integrationtype.Label}
	default:
		return nil, &NotSingularError{integrationtype.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (itq *IntegrationTypeQuery) OnlyX(ctx context.Context) *IntegrationType {
	node, err := itq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only IntegrationType ID in the query.
// Returns a *NotSingularError when more than one IntegrationType ID is found.
// Returns a *NotFoundError when no entities are found.
func (itq *IntegrationTypeQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = itq.Limit(2).IDs(setContextOp(ctx, itq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{integrationtype.Label}
	default:
		err = &NotSingularError{integrationtype.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (itq *IntegrationTypeQuery) OnlyIDX(ctx context.Context) string {
	id, err := itq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of IntegrationTypes.
func (itq *IntegrationTypeQuery) All(ctx context.Context) ([]*IntegrationType, error) {
	ctx = setContextOp(ctx, itq.ctx, ent.OpQueryAll)
	if err := itq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*IntegrationType, *IntegrationTypeQuery]()
	return withInterceptors[[]*IntegrationType](ctx, itq, qr, itq.inters)
}

// AllX is like All, but panics if an error occurs.
func (itq *IntegrationTypeQuery) AllX(ctx context.Context) []*IntegrationType {
	nodes, err := itq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of IntegrationType IDs.
func (itq *IntegrationTypeQuery) IDs(ctx context.Context) (ids []string, err error) {
	if itq.ctx.Unique == nil && itq.path != nil {
		itq.Unique(true)
	}
	ctx = setContextOp(ctx, itq.ctx, ent.OpQueryIDs)
	if err = itq.Select(integrationtype.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (itq *IntegrationTypeQuery) IDsX(ctx context.Context) []string {
	ids, err := itq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (itq *IntegrationTypeQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, itq.ctx, ent.OpQueryCount)
	if err := itq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, itq, querierCount[*IntegrationTypeQuery](), itq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (itq *IntegrationTypeQuery) CountX(ctx context.Context) int {
	count, err := itq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (itq *IntegrationTypeQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, itq.ctx, ent.OpQueryExist)
	switch _, err := itq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (itq *IntegrationTypeQuery) ExistX(ctx context.Context) bool {
	exist, err := itq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the IntegrationTypeQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (itq *IntegrationTypeQuery) Clone() *IntegrationTypeQuery {
	if itq == nil {
		return nil
	}
	return &IntegrationTypeQuery{
		config:           itq.config,
		ctx:              itq.ctx.Clone(),
		order:            append([]integrationtype.OrderOption{}, itq.order...),
		inters:           append([]Interceptor{}, itq.inters...),
		predicates:       append([]predicate.IntegrationType{}, itq.predicates...),
		withIntegrations: itq.withIntegrations.Clone(),
		// clone intermediate query.
		sql:  itq.sql.Clone(),
		path: itq.path,
	}
}

// WithIntegrations tells the query-builder to eager-load the nodes that are connected to
// the "integrations" edge. The optional arguments are used to configure the query builder of the edge.
func (itq *IntegrationTypeQuery) WithIntegrations(opts ...func(*IntegrationsQuery)) *IntegrationTypeQuery {
	query := (&IntegrationsClient{config: itq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	itq.withIntegrations = query
	return itq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		DisplayName string `json:"display_name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.IntegrationType.Query().
//		GroupBy(integrationtype.FieldDisplayName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (itq *IntegrationTypeQuery) GroupBy(field string, fields ...string) *IntegrationTypeGroupBy {
	itq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &IntegrationTypeGroupBy{build: itq}
	grbuild.flds = &itq.ctx.Fields
	grbuild.label = integrationtype.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		DisplayName string `json:"display_name,omitempty"`
//	}
//
//	client.IntegrationType.Query().
//		Select(integrationtype.FieldDisplayName).
//		Scan(ctx, &v)
func (itq *IntegrationTypeQuery) Select(fields ...string) *IntegrationTypeSelect {
	itq.ctx.Fields = append(itq.ctx.Fields, fields...)
	sbuild := &IntegrationTypeSelect{IntegrationTypeQuery: itq}
	sbuild.label = integrationtype.Label
	sbuild.flds, sbuild.scan = &itq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a IntegrationTypeSelect configured with the given aggregations.
func (itq *IntegrationTypeQuery) Aggregate(fns ...AggregateFunc) *IntegrationTypeSelect {
	return itq.Select().Aggregate(fns...)
}

func (itq *IntegrationTypeQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range itq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, itq); err != nil {
				return err
			}
		}
	}
	for _, f := range itq.ctx.Fields {
		if !integrationtype.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if itq.path != nil {
		prev, err := itq.path(ctx)
		if err != nil {
			return err
		}
		itq.sql = prev
	}
	return nil
}

func (itq *IntegrationTypeQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*IntegrationType, error) {
	var (
		nodes       = []*IntegrationType{}
		_spec       = itq.querySpec()
		loadedTypes = [1]bool{
			itq.withIntegrations != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*IntegrationType).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &IntegrationType{config: itq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, itq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := itq.withIntegrations; query != nil {
		if err := itq.loadIntegrations(ctx, query, nodes,
			func(n *IntegrationType) { n.Edges.Integrations = []*Integrations{} },
			func(n *IntegrationType, e *Integrations) { n.Edges.Integrations = append(n.Edges.Integrations, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (itq *IntegrationTypeQuery) loadIntegrations(ctx context.Context, query *IntegrationsQuery, nodes []*IntegrationType, init func(*IntegrationType), assign func(*IntegrationType, *Integrations)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[string]*IntegrationType)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.Integrations(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(integrationtype.IntegrationsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.integration_type_integrations
		if fk == nil {
			return fmt.Errorf(`foreign-key "integration_type_integrations" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "integration_type_integrations" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (itq *IntegrationTypeQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := itq.querySpec()
	_spec.Node.Columns = itq.ctx.Fields
	if len(itq.ctx.Fields) > 0 {
		_spec.Unique = itq.ctx.Unique != nil && *itq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, itq.driver, _spec)
}

func (itq *IntegrationTypeQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(integrationtype.Table, integrationtype.Columns, sqlgraph.NewFieldSpec(integrationtype.FieldID, field.TypeString))
	_spec.From = itq.sql
	if unique := itq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if itq.path != nil {
		_spec.Unique = true
	}
	if fields := itq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, integrationtype.FieldID)
		for i := range fields {
			if fields[i] != integrationtype.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := itq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := itq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := itq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := itq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (itq *IntegrationTypeQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(itq.driver.Dialect())
	t1 := builder.Table(integrationtype.Table)
	columns := itq.ctx.Fields
	if len(columns) == 0 {
		columns = integrationtype.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if itq.sql != nil {
		selector = itq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if itq.ctx.Unique != nil && *itq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range itq.predicates {
		p(selector)
	}
	for _, p := range itq.order {
		p(selector)
	}
	if offset := itq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := itq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// IntegrationTypeGroupBy is the group-by builder for IntegrationType entities.
type IntegrationTypeGroupBy struct {
	selector
	build *IntegrationTypeQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (itgb *IntegrationTypeGroupBy) Aggregate(fns ...AggregateFunc) *IntegrationTypeGroupBy {
	itgb.fns = append(itgb.fns, fns...)
	return itgb
}

// Scan applies the selector query and scans the result into the given value.
func (itgb *IntegrationTypeGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, itgb.build.ctx, ent.OpQueryGroupBy)
	if err := itgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*IntegrationTypeQuery, *IntegrationTypeGroupBy](ctx, itgb.build, itgb, itgb.build.inters, v)
}

func (itgb *IntegrationTypeGroupBy) sqlScan(ctx context.Context, root *IntegrationTypeQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(itgb.fns))
	for _, fn := range itgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*itgb.flds)+len(itgb.fns))
		for _, f := range *itgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*itgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := itgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// IntegrationTypeSelect is the builder for selecting fields of IntegrationType entities.
type IntegrationTypeSelect struct {
	*IntegrationTypeQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (its *IntegrationTypeSelect) Aggregate(fns ...AggregateFunc) *IntegrationTypeSelect {
	its.fns = append(its.fns, fns...)
	return its
}

// Scan applies the selector query and scans the result into the given value.
func (its *IntegrationTypeSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, its.ctx, ent.OpQuerySelect)
	if err := its.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*IntegrationTypeQuery, *IntegrationTypeSelect](ctx, its.IntegrationTypeQuery, its, its.inters, v)
}

func (its *IntegrationTypeSelect) sqlScan(ctx context.Context, root *IntegrationTypeQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(its.fns))
	for _, fn := range its.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*its.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := its.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
