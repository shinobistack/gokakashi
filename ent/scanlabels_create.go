// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/shinobistack/gokakashi/ent/scanlabels"
	"github.com/shinobistack/gokakashi/ent/scans"
)

// ScanLabelsCreate is the builder for creating a ScanLabels entity.
type ScanLabelsCreate struct {
	config
	mutation *ScanLabelsMutation
	hooks    []Hook
}

// SetScanID sets the "scan_id" field.
func (slc *ScanLabelsCreate) SetScanID(u uuid.UUID) *ScanLabelsCreate {
	slc.mutation.SetScanID(u)
	return slc
}

// SetKey sets the "key" field.
func (slc *ScanLabelsCreate) SetKey(s string) *ScanLabelsCreate {
	slc.mutation.SetKey(s)
	return slc
}

// SetValue sets the "value" field.
func (slc *ScanLabelsCreate) SetValue(s string) *ScanLabelsCreate {
	slc.mutation.SetValue(s)
	return slc
}

// SetScan sets the "scan" edge to the Scans entity.
func (slc *ScanLabelsCreate) SetScan(s *Scans) *ScanLabelsCreate {
	return slc.SetScanID(s.ID)
}

// Mutation returns the ScanLabelsMutation object of the builder.
func (slc *ScanLabelsCreate) Mutation() *ScanLabelsMutation {
	return slc.mutation
}

// Save creates the ScanLabels in the database.
func (slc *ScanLabelsCreate) Save(ctx context.Context) (*ScanLabels, error) {
	return withHooks(ctx, slc.sqlSave, slc.mutation, slc.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (slc *ScanLabelsCreate) SaveX(ctx context.Context) *ScanLabels {
	v, err := slc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (slc *ScanLabelsCreate) Exec(ctx context.Context) error {
	_, err := slc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (slc *ScanLabelsCreate) ExecX(ctx context.Context) {
	if err := slc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (slc *ScanLabelsCreate) check() error {
	if _, ok := slc.mutation.ScanID(); !ok {
		return &ValidationError{Name: "scan_id", err: errors.New(`ent: missing required field "ScanLabels.scan_id"`)}
	}
	if _, ok := slc.mutation.Key(); !ok {
		return &ValidationError{Name: "key", err: errors.New(`ent: missing required field "ScanLabels.key"`)}
	}
	if v, ok := slc.mutation.Key(); ok {
		if err := scanlabels.KeyValidator(v); err != nil {
			return &ValidationError{Name: "key", err: fmt.Errorf(`ent: validator failed for field "ScanLabels.key": %w`, err)}
		}
	}
	if _, ok := slc.mutation.Value(); !ok {
		return &ValidationError{Name: "value", err: errors.New(`ent: missing required field "ScanLabels.value"`)}
	}
	if v, ok := slc.mutation.Value(); ok {
		if err := scanlabels.ValueValidator(v); err != nil {
			return &ValidationError{Name: "value", err: fmt.Errorf(`ent: validator failed for field "ScanLabels.value": %w`, err)}
		}
	}
	if len(slc.mutation.ScanIDs()) == 0 {
		return &ValidationError{Name: "scan", err: errors.New(`ent: missing required edge "ScanLabels.scan"`)}
	}
	return nil
}

func (slc *ScanLabelsCreate) sqlSave(ctx context.Context) (*ScanLabels, error) {
	if err := slc.check(); err != nil {
		return nil, err
	}
	_node, _spec := slc.createSpec()
	if err := sqlgraph.CreateNode(ctx, slc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	slc.mutation.id = &_node.ID
	slc.mutation.done = true
	return _node, nil
}

func (slc *ScanLabelsCreate) createSpec() (*ScanLabels, *sqlgraph.CreateSpec) {
	var (
		_node = &ScanLabels{config: slc.config}
		_spec = sqlgraph.NewCreateSpec(scanlabels.Table, sqlgraph.NewFieldSpec(scanlabels.FieldID, field.TypeInt))
	)
	if value, ok := slc.mutation.Key(); ok {
		_spec.SetField(scanlabels.FieldKey, field.TypeString, value)
		_node.Key = value
	}
	if value, ok := slc.mutation.Value(); ok {
		_spec.SetField(scanlabels.FieldValue, field.TypeString, value)
		_node.Value = value
	}
	if nodes := slc.mutation.ScanIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   scanlabels.ScanTable,
			Columns: []string{scanlabels.ScanColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(scans.FieldID, field.TypeUUID),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.ScanID = nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// ScanLabelsCreateBulk is the builder for creating many ScanLabels entities in bulk.
type ScanLabelsCreateBulk struct {
	config
	err      error
	builders []*ScanLabelsCreate
}

// Save creates the ScanLabels entities in the database.
func (slcb *ScanLabelsCreateBulk) Save(ctx context.Context) ([]*ScanLabels, error) {
	if slcb.err != nil {
		return nil, slcb.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(slcb.builders))
	nodes := make([]*ScanLabels, len(slcb.builders))
	mutators := make([]Mutator, len(slcb.builders))
	for i := range slcb.builders {
		func(i int, root context.Context) {
			builder := slcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*ScanLabelsMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, slcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, slcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, slcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (slcb *ScanLabelsCreateBulk) SaveX(ctx context.Context) []*ScanLabels {
	v, err := slcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (slcb *ScanLabelsCreateBulk) Exec(ctx context.Context) error {
	_, err := slcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (slcb *ScanLabelsCreateBulk) ExecX(ctx context.Context) {
	if err := slcb.Exec(ctx); err != nil {
		panic(err)
	}
}
