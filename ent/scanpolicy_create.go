// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/shinobistack/gokakashi/ent/scanpolicy"
)

// ScanPolicyCreate is the builder for creating a ScanPolicy entity.
type ScanPolicyCreate struct {
	config
	mutation *ScanPolicyMutation
	hooks    []Hook
}

// SetName sets the "name" field.
func (spc *ScanPolicyCreate) SetName(s string) *ScanPolicyCreate {
	spc.mutation.SetName(s)
	return spc
}

// SetTriggerType sets the "trigger_type" field.
func (spc *ScanPolicyCreate) SetTriggerType(s string) *ScanPolicyCreate {
	spc.mutation.SetTriggerType(s)
	return spc
}

// Mutation returns the ScanPolicyMutation object of the builder.
func (spc *ScanPolicyCreate) Mutation() *ScanPolicyMutation {
	return spc.mutation
}

// Save creates the ScanPolicy in the database.
func (spc *ScanPolicyCreate) Save(ctx context.Context) (*ScanPolicy, error) {
	return withHooks(ctx, spc.sqlSave, spc.mutation, spc.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (spc *ScanPolicyCreate) SaveX(ctx context.Context) *ScanPolicy {
	v, err := spc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (spc *ScanPolicyCreate) Exec(ctx context.Context) error {
	_, err := spc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (spc *ScanPolicyCreate) ExecX(ctx context.Context) {
	if err := spc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (spc *ScanPolicyCreate) check() error {
	if _, ok := spc.mutation.Name(); !ok {
		return &ValidationError{Name: "name", err: errors.New(`ent: missing required field "ScanPolicy.name"`)}
	}
	if _, ok := spc.mutation.TriggerType(); !ok {
		return &ValidationError{Name: "trigger_type", err: errors.New(`ent: missing required field "ScanPolicy.trigger_type"`)}
	}
	return nil
}

func (spc *ScanPolicyCreate) sqlSave(ctx context.Context) (*ScanPolicy, error) {
	if err := spc.check(); err != nil {
		return nil, err
	}
	_node, _spec := spc.createSpec()
	if err := sqlgraph.CreateNode(ctx, spc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	spc.mutation.id = &_node.ID
	spc.mutation.done = true
	return _node, nil
}

func (spc *ScanPolicyCreate) createSpec() (*ScanPolicy, *sqlgraph.CreateSpec) {
	var (
		_node = &ScanPolicy{config: spc.config}
		_spec = sqlgraph.NewCreateSpec(scanpolicy.Table, sqlgraph.NewFieldSpec(scanpolicy.FieldID, field.TypeInt))
	)
	if value, ok := spc.mutation.Name(); ok {
		_spec.SetField(scanpolicy.FieldName, field.TypeString, value)
		_node.Name = value
	}
	if value, ok := spc.mutation.TriggerType(); ok {
		_spec.SetField(scanpolicy.FieldTriggerType, field.TypeString, value)
		_node.TriggerType = value
	}
	return _node, _spec
}

// ScanPolicyCreateBulk is the builder for creating many ScanPolicy entities in bulk.
type ScanPolicyCreateBulk struct {
	config
	err      error
	builders []*ScanPolicyCreate
}

// Save creates the ScanPolicy entities in the database.
func (spcb *ScanPolicyCreateBulk) Save(ctx context.Context) ([]*ScanPolicy, error) {
	if spcb.err != nil {
		return nil, spcb.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(spcb.builders))
	nodes := make([]*ScanPolicy, len(spcb.builders))
	mutators := make([]Mutator, len(spcb.builders))
	for i := range spcb.builders {
		func(i int, root context.Context) {
			builder := spcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*ScanPolicyMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, spcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, spcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, spcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (spcb *ScanPolicyCreateBulk) SaveX(ctx context.Context) []*ScanPolicy {
	v, err := spcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (spcb *ScanPolicyCreateBulk) Exec(ctx context.Context) error {
	_, err := spcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (spcb *ScanPolicyCreateBulk) ExecX(ctx context.Context) {
	if err := spcb.Exec(ctx); err != nil {
		panic(err)
	}
}
