// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/shinobistack/gokakashi/ent/policies"
	"github.com/shinobistack/gokakashi/ent/policylabels"
	"github.com/shinobistack/gokakashi/ent/predicate"
)

// PoliciesQuery is the builder for querying Policies entities.
type PoliciesQuery struct {
	config
	ctx              *QueryContext
	order            []policies.OrderOption
	inters           []Interceptor
	predicates       []predicate.Policies
	withPolicyLabels *PolicyLabelsQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PoliciesQuery builder.
func (pq *PoliciesQuery) Where(ps ...predicate.Policies) *PoliciesQuery {
	pq.predicates = append(pq.predicates, ps...)
	return pq
}

// Limit the number of records to be returned by this query.
func (pq *PoliciesQuery) Limit(limit int) *PoliciesQuery {
	pq.ctx.Limit = &limit
	return pq
}

// Offset to start from.
func (pq *PoliciesQuery) Offset(offset int) *PoliciesQuery {
	pq.ctx.Offset = &offset
	return pq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (pq *PoliciesQuery) Unique(unique bool) *PoliciesQuery {
	pq.ctx.Unique = &unique
	return pq
}

// Order specifies how the records should be ordered.
func (pq *PoliciesQuery) Order(o ...policies.OrderOption) *PoliciesQuery {
	pq.order = append(pq.order, o...)
	return pq
}

// QueryPolicyLabels chains the current query on the "policy_labels" edge.
func (pq *PoliciesQuery) QueryPolicyLabels() *PolicyLabelsQuery {
	query := (&PolicyLabelsClient{config: pq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(policies.Table, policies.FieldID, selector),
			sqlgraph.To(policylabels.Table, policylabels.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, policies.PolicyLabelsTable, policies.PolicyLabelsColumn),
		)
		fromU = sqlgraph.SetNeighbors(pq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Policies entity from the query.
// Returns a *NotFoundError when no Policies was found.
func (pq *PoliciesQuery) First(ctx context.Context) (*Policies, error) {
	nodes, err := pq.Limit(1).All(setContextOp(ctx, pq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{policies.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (pq *PoliciesQuery) FirstX(ctx context.Context) *Policies {
	node, err := pq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Policies ID from the query.
// Returns a *NotFoundError when no Policies ID was found.
func (pq *PoliciesQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = pq.Limit(1).IDs(setContextOp(ctx, pq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{policies.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (pq *PoliciesQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := pq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Policies entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Policies entity is found.
// Returns a *NotFoundError when no Policies entities are found.
func (pq *PoliciesQuery) Only(ctx context.Context) (*Policies, error) {
	nodes, err := pq.Limit(2).All(setContextOp(ctx, pq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{policies.Label}
	default:
		return nil, &NotSingularError{policies.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (pq *PoliciesQuery) OnlyX(ctx context.Context) *Policies {
	node, err := pq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Policies ID in the query.
// Returns a *NotSingularError when more than one Policies ID is found.
// Returns a *NotFoundError when no entities are found.
func (pq *PoliciesQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = pq.Limit(2).IDs(setContextOp(ctx, pq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{policies.Label}
	default:
		err = &NotSingularError{policies.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (pq *PoliciesQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := pq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of PoliciesSlice.
func (pq *PoliciesQuery) All(ctx context.Context) ([]*Policies, error) {
	ctx = setContextOp(ctx, pq.ctx, ent.OpQueryAll)
	if err := pq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Policies, *PoliciesQuery]()
	return withInterceptors[[]*Policies](ctx, pq, qr, pq.inters)
}

// AllX is like All, but panics if an error occurs.
func (pq *PoliciesQuery) AllX(ctx context.Context) []*Policies {
	nodes, err := pq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Policies IDs.
func (pq *PoliciesQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if pq.ctx.Unique == nil && pq.path != nil {
		pq.Unique(true)
	}
	ctx = setContextOp(ctx, pq.ctx, ent.OpQueryIDs)
	if err = pq.Select(policies.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (pq *PoliciesQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := pq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (pq *PoliciesQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, pq.ctx, ent.OpQueryCount)
	if err := pq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, pq, querierCount[*PoliciesQuery](), pq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (pq *PoliciesQuery) CountX(ctx context.Context) int {
	count, err := pq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (pq *PoliciesQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, pq.ctx, ent.OpQueryExist)
	switch _, err := pq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (pq *PoliciesQuery) ExistX(ctx context.Context) bool {
	exist, err := pq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PoliciesQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (pq *PoliciesQuery) Clone() *PoliciesQuery {
	if pq == nil {
		return nil
	}
	return &PoliciesQuery{
		config:           pq.config,
		ctx:              pq.ctx.Clone(),
		order:            append([]policies.OrderOption{}, pq.order...),
		inters:           append([]Interceptor{}, pq.inters...),
		predicates:       append([]predicate.Policies{}, pq.predicates...),
		withPolicyLabels: pq.withPolicyLabels.Clone(),
		// clone intermediate query.
		sql:  pq.sql.Clone(),
		path: pq.path,
	}
}

// WithPolicyLabels tells the query-builder to eager-load the nodes that are connected to
// the "policy_labels" edge. The optional arguments are used to configure the query builder of the edge.
func (pq *PoliciesQuery) WithPolicyLabels(opts ...func(*PolicyLabelsQuery)) *PoliciesQuery {
	query := (&PolicyLabelsClient{config: pq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	pq.withPolicyLabels = query
	return pq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Policies.Query().
//		GroupBy(policies.FieldName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (pq *PoliciesQuery) GroupBy(field string, fields ...string) *PoliciesGroupBy {
	pq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &PoliciesGroupBy{build: pq}
	grbuild.flds = &pq.ctx.Fields
	grbuild.label = policies.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//	}
//
//	client.Policies.Query().
//		Select(policies.FieldName).
//		Scan(ctx, &v)
func (pq *PoliciesQuery) Select(fields ...string) *PoliciesSelect {
	pq.ctx.Fields = append(pq.ctx.Fields, fields...)
	sbuild := &PoliciesSelect{PoliciesQuery: pq}
	sbuild.label = policies.Label
	sbuild.flds, sbuild.scan = &pq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PoliciesSelect configured with the given aggregations.
func (pq *PoliciesQuery) Aggregate(fns ...AggregateFunc) *PoliciesSelect {
	return pq.Select().Aggregate(fns...)
}

func (pq *PoliciesQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range pq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, pq); err != nil {
				return err
			}
		}
	}
	for _, f := range pq.ctx.Fields {
		if !policies.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if pq.path != nil {
		prev, err := pq.path(ctx)
		if err != nil {
			return err
		}
		pq.sql = prev
	}
	return nil
}

func (pq *PoliciesQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Policies, error) {
	var (
		nodes       = []*Policies{}
		_spec       = pq.querySpec()
		loadedTypes = [1]bool{
			pq.withPolicyLabels != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Policies).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Policies{config: pq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, pq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := pq.withPolicyLabels; query != nil {
		if err := pq.loadPolicyLabels(ctx, query, nodes,
			func(n *Policies) { n.Edges.PolicyLabels = []*PolicyLabels{} },
			func(n *Policies, e *PolicyLabels) { n.Edges.PolicyLabels = append(n.Edges.PolicyLabels, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (pq *PoliciesQuery) loadPolicyLabels(ctx context.Context, query *PolicyLabelsQuery, nodes []*Policies, init func(*Policies), assign func(*Policies, *PolicyLabels)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*Policies)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(policylabels.FieldPolicyID)
	}
	query.Where(predicate.PolicyLabels(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(policies.PolicyLabelsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.PolicyID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "policy_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (pq *PoliciesQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := pq.querySpec()
	_spec.Node.Columns = pq.ctx.Fields
	if len(pq.ctx.Fields) > 0 {
		_spec.Unique = pq.ctx.Unique != nil && *pq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, pq.driver, _spec)
}

func (pq *PoliciesQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(policies.Table, policies.Columns, sqlgraph.NewFieldSpec(policies.FieldID, field.TypeUUID))
	_spec.From = pq.sql
	if unique := pq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if pq.path != nil {
		_spec.Unique = true
	}
	if fields := pq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, policies.FieldID)
		for i := range fields {
			if fields[i] != policies.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := pq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := pq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := pq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := pq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (pq *PoliciesQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(pq.driver.Dialect())
	t1 := builder.Table(policies.Table)
	columns := pq.ctx.Fields
	if len(columns) == 0 {
		columns = policies.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if pq.sql != nil {
		selector = pq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if pq.ctx.Unique != nil && *pq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range pq.predicates {
		p(selector)
	}
	for _, p := range pq.order {
		p(selector)
	}
	if offset := pq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := pq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// PoliciesGroupBy is the group-by builder for Policies entities.
type PoliciesGroupBy struct {
	selector
	build *PoliciesQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (pgb *PoliciesGroupBy) Aggregate(fns ...AggregateFunc) *PoliciesGroupBy {
	pgb.fns = append(pgb.fns, fns...)
	return pgb
}

// Scan applies the selector query and scans the result into the given value.
func (pgb *PoliciesGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pgb.build.ctx, ent.OpQueryGroupBy)
	if err := pgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PoliciesQuery, *PoliciesGroupBy](ctx, pgb.build, pgb, pgb.build.inters, v)
}

func (pgb *PoliciesGroupBy) sqlScan(ctx context.Context, root *PoliciesQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(pgb.fns))
	for _, fn := range pgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*pgb.flds)+len(pgb.fns))
		for _, f := range *pgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*pgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// PoliciesSelect is the builder for selecting fields of Policies entities.
type PoliciesSelect struct {
	*PoliciesQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ps *PoliciesSelect) Aggregate(fns ...AggregateFunc) *PoliciesSelect {
	ps.fns = append(ps.fns, fns...)
	return ps
}

// Scan applies the selector query and scans the result into the given value.
func (ps *PoliciesSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ps.ctx, ent.OpQuerySelect)
	if err := ps.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PoliciesQuery, *PoliciesSelect](ctx, ps.PoliciesQuery, ps, ps.inters, v)
}

func (ps *PoliciesSelect) sqlScan(ctx context.Context, root *PoliciesQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ps.fns))
	for _, fn := range ps.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ps.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ps.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
