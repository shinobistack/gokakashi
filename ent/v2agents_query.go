// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/shinobistack/gokakashi/ent/predicate"
	"github.com/shinobistack/gokakashi/ent/v2agents"
)

// V2AgentsQuery is the builder for querying V2Agents entities.
type V2AgentsQuery struct {
	config
	ctx        *QueryContext
	order      []v2agents.OrderOption
	inters     []Interceptor
	predicates []predicate.V2Agents
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the V2AgentsQuery builder.
func (vq *V2AgentsQuery) Where(ps ...predicate.V2Agents) *V2AgentsQuery {
	vq.predicates = append(vq.predicates, ps...)
	return vq
}

// Limit the number of records to be returned by this query.
func (vq *V2AgentsQuery) Limit(limit int) *V2AgentsQuery {
	vq.ctx.Limit = &limit
	return vq
}

// Offset to start from.
func (vq *V2AgentsQuery) Offset(offset int) *V2AgentsQuery {
	vq.ctx.Offset = &offset
	return vq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (vq *V2AgentsQuery) Unique(unique bool) *V2AgentsQuery {
	vq.ctx.Unique = &unique
	return vq
}

// Order specifies how the records should be ordered.
func (vq *V2AgentsQuery) Order(o ...v2agents.OrderOption) *V2AgentsQuery {
	vq.order = append(vq.order, o...)
	return vq
}

// First returns the first V2Agents entity from the query.
// Returns a *NotFoundError when no V2Agents was found.
func (vq *V2AgentsQuery) First(ctx context.Context) (*V2Agents, error) {
	nodes, err := vq.Limit(1).All(setContextOp(ctx, vq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{v2agents.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (vq *V2AgentsQuery) FirstX(ctx context.Context) *V2Agents {
	node, err := vq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first V2Agents ID from the query.
// Returns a *NotFoundError when no V2Agents ID was found.
func (vq *V2AgentsQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = vq.Limit(1).IDs(setContextOp(ctx, vq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{v2agents.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (vq *V2AgentsQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := vq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single V2Agents entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one V2Agents entity is found.
// Returns a *NotFoundError when no V2Agents entities are found.
func (vq *V2AgentsQuery) Only(ctx context.Context) (*V2Agents, error) {
	nodes, err := vq.Limit(2).All(setContextOp(ctx, vq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{v2agents.Label}
	default:
		return nil, &NotSingularError{v2agents.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (vq *V2AgentsQuery) OnlyX(ctx context.Context) *V2Agents {
	node, err := vq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only V2Agents ID in the query.
// Returns a *NotSingularError when more than one V2Agents ID is found.
// Returns a *NotFoundError when no entities are found.
func (vq *V2AgentsQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = vq.Limit(2).IDs(setContextOp(ctx, vq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{v2agents.Label}
	default:
		err = &NotSingularError{v2agents.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (vq *V2AgentsQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := vq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of V2AgentsSlice.
func (vq *V2AgentsQuery) All(ctx context.Context) ([]*V2Agents, error) {
	ctx = setContextOp(ctx, vq.ctx, ent.OpQueryAll)
	if err := vq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*V2Agents, *V2AgentsQuery]()
	return withInterceptors[[]*V2Agents](ctx, vq, qr, vq.inters)
}

// AllX is like All, but panics if an error occurs.
func (vq *V2AgentsQuery) AllX(ctx context.Context) []*V2Agents {
	nodes, err := vq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of V2Agents IDs.
func (vq *V2AgentsQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if vq.ctx.Unique == nil && vq.path != nil {
		vq.Unique(true)
	}
	ctx = setContextOp(ctx, vq.ctx, ent.OpQueryIDs)
	if err = vq.Select(v2agents.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (vq *V2AgentsQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := vq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (vq *V2AgentsQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, vq.ctx, ent.OpQueryCount)
	if err := vq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, vq, querierCount[*V2AgentsQuery](), vq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (vq *V2AgentsQuery) CountX(ctx context.Context) int {
	count, err := vq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (vq *V2AgentsQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, vq.ctx, ent.OpQueryExist)
	switch _, err := vq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (vq *V2AgentsQuery) ExistX(ctx context.Context) bool {
	exist, err := vq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the V2AgentsQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (vq *V2AgentsQuery) Clone() *V2AgentsQuery {
	if vq == nil {
		return nil
	}
	return &V2AgentsQuery{
		config:     vq.config,
		ctx:        vq.ctx.Clone(),
		order:      append([]v2agents.OrderOption{}, vq.order...),
		inters:     append([]Interceptor{}, vq.inters...),
		predicates: append([]predicate.V2Agents{}, vq.predicates...),
		// clone intermediate query.
		sql:  vq.sql.Clone(),
		path: vq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Status string `json:"status,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.V2Agents.Query().
//		GroupBy(v2agents.FieldStatus).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (vq *V2AgentsQuery) GroupBy(field string, fields ...string) *V2AgentsGroupBy {
	vq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &V2AgentsGroupBy{build: vq}
	grbuild.flds = &vq.ctx.Fields
	grbuild.label = v2agents.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Status string `json:"status,omitempty"`
//	}
//
//	client.V2Agents.Query().
//		Select(v2agents.FieldStatus).
//		Scan(ctx, &v)
func (vq *V2AgentsQuery) Select(fields ...string) *V2AgentsSelect {
	vq.ctx.Fields = append(vq.ctx.Fields, fields...)
	sbuild := &V2AgentsSelect{V2AgentsQuery: vq}
	sbuild.label = v2agents.Label
	sbuild.flds, sbuild.scan = &vq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a V2AgentsSelect configured with the given aggregations.
func (vq *V2AgentsQuery) Aggregate(fns ...AggregateFunc) *V2AgentsSelect {
	return vq.Select().Aggregate(fns...)
}

func (vq *V2AgentsQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range vq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, vq); err != nil {
				return err
			}
		}
	}
	for _, f := range vq.ctx.Fields {
		if !v2agents.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if vq.path != nil {
		prev, err := vq.path(ctx)
		if err != nil {
			return err
		}
		vq.sql = prev
	}
	return nil
}

func (vq *V2AgentsQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*V2Agents, error) {
	var (
		nodes = []*V2Agents{}
		_spec = vq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*V2Agents).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &V2Agents{config: vq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, vq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (vq *V2AgentsQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := vq.querySpec()
	_spec.Node.Columns = vq.ctx.Fields
	if len(vq.ctx.Fields) > 0 {
		_spec.Unique = vq.ctx.Unique != nil && *vq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, vq.driver, _spec)
}

func (vq *V2AgentsQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(v2agents.Table, v2agents.Columns, sqlgraph.NewFieldSpec(v2agents.FieldID, field.TypeUUID))
	_spec.From = vq.sql
	if unique := vq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if vq.path != nil {
		_spec.Unique = true
	}
	if fields := vq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, v2agents.FieldID)
		for i := range fields {
			if fields[i] != v2agents.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := vq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := vq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := vq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := vq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (vq *V2AgentsQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(vq.driver.Dialect())
	t1 := builder.Table(v2agents.Table)
	columns := vq.ctx.Fields
	if len(columns) == 0 {
		columns = v2agents.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if vq.sql != nil {
		selector = vq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if vq.ctx.Unique != nil && *vq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range vq.predicates {
		p(selector)
	}
	for _, p := range vq.order {
		p(selector)
	}
	if offset := vq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := vq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// V2AgentsGroupBy is the group-by builder for V2Agents entities.
type V2AgentsGroupBy struct {
	selector
	build *V2AgentsQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (vgb *V2AgentsGroupBy) Aggregate(fns ...AggregateFunc) *V2AgentsGroupBy {
	vgb.fns = append(vgb.fns, fns...)
	return vgb
}

// Scan applies the selector query and scans the result into the given value.
func (vgb *V2AgentsGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, vgb.build.ctx, ent.OpQueryGroupBy)
	if err := vgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*V2AgentsQuery, *V2AgentsGroupBy](ctx, vgb.build, vgb, vgb.build.inters, v)
}

func (vgb *V2AgentsGroupBy) sqlScan(ctx context.Context, root *V2AgentsQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(vgb.fns))
	for _, fn := range vgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*vgb.flds)+len(vgb.fns))
		for _, f := range *vgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*vgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := vgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// V2AgentsSelect is the builder for selecting fields of V2Agents entities.
type V2AgentsSelect struct {
	*V2AgentsQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (vs *V2AgentsSelect) Aggregate(fns ...AggregateFunc) *V2AgentsSelect {
	vs.fns = append(vs.fns, fns...)
	return vs
}

// Scan applies the selector query and scans the result into the given value.
func (vs *V2AgentsSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, vs.ctx, ent.OpQuerySelect)
	if err := vs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*V2AgentsQuery, *V2AgentsSelect](ctx, vs.V2AgentsQuery, vs, vs.inters, v)
}

func (vs *V2AgentsSelect) sqlScan(ctx context.Context, root *V2AgentsQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(vs.fns))
	for _, fn := range vs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*vs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := vs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
