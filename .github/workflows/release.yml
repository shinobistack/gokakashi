name: Release GoKakashi Binaries

on:
  release:
    types: [created]
  workflow_dispatch:
    inputs:
      tag:
        description: "Tag for the release (e.g., v0.1.0)"
        required: true
      name:
        description: "Name the release (e.g., v0.1.0 Chidori)"
        required: false

jobs:
  build-and-release-binaries:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: "1.23"

      - name: Tidy Up Go Modules
        run: go mod tidy

      - name: Install Dependencies (webapp)
        run: npm install
        working-directory: ./webapp

      - name: Build Binaries
        run: |
          mkdir -p dist
          GOOS=linux GOARCH=amd64 go build -o dist/gokakashi-agent-linux-amd64 ./cmd/
          GOOS=linux GOARCH=arm64 go build -o dist/gokakashi-agent-linux-arm64 ./cmd/
          GOOS=darwin GOARCH=amd64 go build -o dist/gokakashi-agent-mac-amd64 ./cmd/
          GOOS=darwin GOARCH=arm64 go build -o dist/gokakashi-agent-mac-arm64 ./cmd/
          GOOS=windows GOARCH=amd64 go build -o dist/gokakashi-agent-windows-amd64.exe ./cmd/

      - name: Upload Release Binaries
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ github.event.inputs.tag }}
          files: dist/*
          name: ${{ github.event.inputs.name }}
          body: "🚀"